name: PDF Build
on: [push]

jobs:
  paper:
    runs-on: ubuntu-latest
    name: Build PDF
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Pandoc
        uses: docker://pandoc/pandoc:latest
        with:
          args: >-
            paper.md
            --citeproc
            --output=paper.pdf
      
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: paper
          path: paper-final.pdf